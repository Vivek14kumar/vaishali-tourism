/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://vaishalitourism.in',
  generateRobotsTxt: true, // (optional) generate robots.txt
  sitemapSize: 5000,       // optional: split sitemap if more than 5000 URLs
};
